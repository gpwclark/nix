with import <nixpkgs> {}; [
  coreutils-full
  fd
  jq
  neovim
  nodejs
  pwgen
  python3
  universal-ctags
  ripgrep # better grep
  shellcheck # spell check on the cli
  socat # socket cat
  tmux # terminal multiplexer
  tree # better ls for directory hierarchies
  git #scm
  mr # scm
  zsh-syntax-highlighting # more zsh
  lsd # better ls
  htop # better top
  emacs # ONLY for clj/cljs development
  fortune #fun. random. text.
  gradle # build tool
  maven  # build tool
  sl #get on the train.
  wget # simple http get
  curl # complex http get
  youtube-dl # need a screensaver :)
  leiningen # clojure build tool
  mplayer  # all things media
  feh  # light weight previewer
  graphviz # graph visualization/generations software
  rlwrap # important for wrapping cli programs
  fzf # ctrl-r on crack
  git-crypt #  Transparent file encryption in Git
  syncthing # to sync things across computers.
  meld # better visual difftool.
  vcsh # config management
  cmake # build tool
  git-crypt # commit secerts in the open!
  nerdfonts # fonts
  nerd-font-patcher # fonts for lsd
  iterm2 # terminal
  sbcl # steel bank common lisp
  rclone # rsync but for cloud backup.
  git-crypt # encryption for secrets in git repos.
  git-repo # android multi-git repo management tool
  docker-compose # docker orechestration tool
  gnupg # good protection
  pinentry # for gnupg passphrase entry on mac
  sqlite # very useful database for almost everyone because RU really at scale?
]
